// import * as C from '../constants.js';


/*
	Item
	Plants will drop items on harvest (automatically added to player inventory?)
	For now don't add icons, but have the item inventory available as a list 

*/
export default class Item {
	constructor() {

	}
}